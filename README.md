# Nail-Disease-Detection
Deep learning-based nail disease classification using VGG16 transfer learning. The model is trained on a custom dataset of 17 classes with data augmentation, class balancing, and fine-tuning. Achieves high accuracy with performance evaluation using confusion matrix and classification metrics.
